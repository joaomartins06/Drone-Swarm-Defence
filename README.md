# Drone-Swarm-Defence

2D simulation environment for analyzing cost-exchange dynamics between
decentralized drone swarms and radar-tracking interceptor networks.

## Setup
```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Status
Pre-work phase: foundational reading and repo setup.
